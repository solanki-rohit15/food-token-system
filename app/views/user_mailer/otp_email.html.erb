<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Inter', sans-serif; background: #f4f6f8; margin: 0; padding: 20px; }
    .container { max-width: 480px; margin: 0 auto; background: #fff; border-radius: 16px; overflow: hidden; box-shadow: 0 4px 24px rgba(0,0,0,0.08); }
    .header { background: linear-gradient(135deg, #1a7a4a, #0d5c36); padding: 32px 24px; text-align: center; color: #fff; }
    .logo { font-size: 40px; margin-bottom: 8px; }
    .header h1 { font-size: 22px; font-weight: 700; margin: 0; }
    .body { padding: 32px 24px; }
    .otp-box { background: #f0fdf4; border: 2px solid #d1fae5; border-radius: 12px; padding: 20px; text-align: center; margin: 20px 0; }
    .otp-code { font-size: 38px; font-weight: 800; letter-spacing: 8px; color: #1a7a4a; }
    .otp-label { font-size: 12px; color: #6b7280; margin-top: 4px; }
    .footer { background: #f9fafb; padding: 16px 24px; text-align: center; font-size: 11px; color: #9ca3af; }
    p { color: #374151; line-height: 1.6; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">🍽️</div>
      <h1>FoodToken</h1>
    </div>
    <div class="body">
      <p>Hi <strong><%= @user.name %></strong>,</p>
      <p>Your one-time password for FoodToken login:</p>
      <div class="otp-box">
        <div class="otp-code"><%= @otp_code %></div>
        <div class="otp-label">Valid for <%= @expires_in %></div>
      </div>
      <p>Enter this code on the login page to proceed. Do not share this OTP with anyone.</p>
      <p>If you didn't request this, please ignore this email.</p>
    </div>
    <div class="footer">
      © <%= Date.current.year %> FoodToken System. Secure Cafeteria Management.
    </div>
  </div>
</body>
</html>
